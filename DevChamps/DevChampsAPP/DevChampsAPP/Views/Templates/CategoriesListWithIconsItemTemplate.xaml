<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="DevChampsAPP.Views.Templates.CategoriesListWithIconsItemTemplate"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DevChampsAPP;assembly=DevChampsAPP"
    BackgroundColor="Black">

    <StackLayout
        BackgroundColor="White"
        HorizontalOptions="FillAndExpand"
        Orientation="Horizontal"
        Spacing="20"
        VerticalOptions="FillAndExpand">

        <Grid
            BackgroundColor="#33000000"
            HorizontalOptions="Start"
            VerticalOptions="FillAndExpand"
            WidthRequest="80">

            <Label
                FontSize="40"
                HorizontalOptions="Center"
                Opacity=".2"
                Style="{x:Static local:AppConstants.FontIcon}"
                Text="{Binding Ico}"
                TextColor="White"
                VerticalOptions="Center" />
        </Grid>

        <StackLayout Spacing="2" VerticalOptions="Center">
            <Label Text="{Binding Nome}" TextColor="White" />
        </StackLayout>

    </StackLayout>
</ContentView>